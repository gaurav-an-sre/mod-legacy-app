# Extracted services

This directory intentionally stays empty. Cursor cloud agents populate it as
they extract slices from the legacy monolith; the platform and parity harness
must work before any extracted service exists.
